import { StyleSheet } from "react-native";

const ThesisStyle = StyleSheet.create({
    text: {
        fontSize: 10,
        textAlign: "left",
        color: "white",
        fontWeight: "bold",
        padding: 10,
        margin: 10
    }
});

export default ThesisStyle;